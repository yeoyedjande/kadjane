/** Développement : le backend tourne via `docker compose up -d`. */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api/v1',
  filesBaseUrl: 'http://localhost:8000',
  appName: 'Kadjane Admin',
};
